/**
 * CJS handler for Vercel serverless functions.
 * CACHE-BUSTER: filename changed to force Vercel redeploy (bypass function cache).
 * Build v5: 0 >>> tokens, 0 import.meta, 0 await import() — pure CJS.
 */
